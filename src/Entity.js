import Manager from './Manager';

class Entity {
    constructor (x, y) {
       // console.log("ENT GAME", game)
        this.x = 1000;
        this.y = 400;
       // this._container = game.add.group();
        //this._game = game;
    }

  /*  get container () {
        return _container;
    }

    get add () {
        return this._game.add;
    }
*/
}

export default Entity;